<!DOCTYPE html>
<html>
<head>
    <title>Blog Posts</title>
</head>
<body>
    <p>List of posts:</p>
    <ul>
        {% for post in collections.post -%}
        <li><a href="{{ post.url }}">{{ post.data.title }}, {{ post.data.date | postDate }}</a></li>
        {% endfor -%}
    </ul>
</body>
</html>